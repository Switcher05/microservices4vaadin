server:
  port: 8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:8761}/eureka/ 

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/khauser/microservices4vaadin-config
